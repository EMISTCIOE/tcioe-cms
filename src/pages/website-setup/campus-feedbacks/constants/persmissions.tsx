import { IRequiredPermission } from '@/globals';

export const campusFeedbacksPermissions: IRequiredPermission = {
  view: 'view_campus_feedbacks',
  edit: 'edit_campus_feedbacks',
  add: 'add_campus_feedbacks',
  delete: 'delete_campus_feedbacks'
};
