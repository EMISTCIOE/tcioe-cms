import { IRequiredPermission } from '@/globals';

export const campusEventsPermissions: IRequiredPermission = {
  view: 'view_campus_events',
  edit: 'edit_campus_events',
  add: 'add_campus_events',
  delete: 'delete_campus_events'
};
