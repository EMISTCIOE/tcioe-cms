import { IRequiredPermission } from '@/globals';

export const campusReportsPermissions: IRequiredPermission = {
  view: 'view_campus_reports',
  edit: 'edit_campus_reports',
  add: 'add_campus_reports',
  delete: 'delete_campus_reports'
};
