import { IRequiredPermission } from '@/globals';

export const campusUnionsPermissions: IRequiredPermission = {
  view: 'view_campus_unions',
  edit: 'edit_campus_unions',
  add: 'add_campus_unions',
  delete: 'delete_campus_unions'
};
